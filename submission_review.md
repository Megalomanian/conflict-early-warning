# 投稿前评估：现存问题与待补实验

评估日期：2026-08-04　分支：`agent/leakage-free-gpu-rerun`

---

## 一、最核心的问题：`main.tex` 不是最好的版本

仓库里有两个版本：

- **`main.tex`**（当前编译的主版本）
- **`main_feasibility.tex`**（8 月 3 日写的）

### `main.tex` 的真实数据结论是全负结果

| 实验 | 结果 |
|---|---|
| 知乎冲突指数预测 | R² ≈ 0 |
| 微博密集事件冲突指数 | R² = 0.018 |
| 合成数据 CNN-BiLSTM | R² = 0.809 ± 0.010 |
| 合成数据 BiGRU（基线） | R² = 0.811 ± 0.009 |
| 提前预警时间 | −1.7 ± 3.2 bins（负值 = 滞后） |

也就是说：**方法本身没有一处在真实数据上有效**。CNN-BiLSTM 在合成数据上与更简单的 BiGRU 统计不可区分（Δ R² = −0.002，在一个标准差内），提前量还是负的。

按现在的样子投 IEEE，我判断大概率被拒。审稿人会问：主要贡献是什么？合成数据上不比 BiGRU 好，真实数据上完全失效，提前量为负——框架的价值在哪里？

"诚实的负结果"本身有价值，但负结果论文的门槛更高：需要系统的消融来证明"不是实现得差，而是这类信号根本不行"，而且通常发在方法论或复现类期刊，不是 IEEE 的常规投稿。

### `main_feasibility.tex` 已经有正结果

用的是 `optimize_real_signal_v2.py` 里那套**因果清洗 + 风险状态分类**：

| 指标 | 本方法（logistic） | Persistence 基线 |
|---|---|---|
| AUC | 0.891 | 0.896 |
| Precision | 0.738 | 0.926 |
| Recall | **0.822** | 0.584 |
| F1 | **0.778** | 0.716 |

- 20 个知乎主题，1706 个测试窗口
- 主题级配对 bootstrap：F1 提升 **+0.062**（95% CI [0.018, 0.108]），recall 提升 **+0.241**（[0.173, 0.317]）——两个置信区间均不含零
- 20 个主题中 15 个 F1 改善、18 个 recall 改善
- 在 70、80、90 分位三种风险定义下结论均为正

这些数字已与 `experiment_results_v2/signal_cleaning_results.json` 核对一致，且 `test_was_not_used_for_selection: true`，选型确实只用了验证集。

### 关键在于换了预测目标

从"预测冲突指数的**精确数值**"（回归，做不到）改成"预测未来 6 个 bin 内**是否会超过**训练期 80 分位阈值"（分类，做得到）。

这是合理且诚实的重构——预警系统本来只需要知道"会不会出事"，不需要知道指数具体是 0.63 还是 0.67。而且它保留了负结果作为边界分析（连续值回归 ridge R² = 0.465 仍低于 persistence 的 0.640），这反而让论文更可信。

> **建议：以 `main_feasibility.tex` 为投稿主线，放弃 `main.tex` 的全负结果版本。**

---

## 二、需要补的实验

前两项是投稿前**必须**做的。

### 1. 冲突指数的构念效度验证（最关键的短板）

这是目前最容易被一击致命的地方。冲突指数完全建立在预训练模型上，而且是错配的：用一个**五星商品评价情感模型**（`bert-multilingual-sentiment`，1 星 → 高攻击性）来测量论证性文本中的攻击强度。审稿人几乎肯定会问：这个分数真的在测量"冲突"吗？Limitations 第 1 条自己也承认了。

**现状问题**：`wikipedia_detox/` 下两个 TSV 都只有 6 行——只是表头和几行样本，真实数据没下下来。

**要做的事**：

- **英文侧**：补全 Wikipedia Detox 数据。该数据集有人工标注的 personal attack 概率，可直接验证攻击强度分量与人工判断的一致性。报告 attack 分量与人工标注的 Spearman 相关系数与 AUC。
- **中文侧**：从知乎数据分层抽样 300–500 条评论（按冲突指数分数分层，覆盖高低两端），2–3 人独立标注冲突强度，报告标注者间一致性（Krippendorff's α 或 Cohen's κ）以及指数与人工标注的相关性。

500 条的标注量一两天能完成，但它把"我们**假设**这个指数测量了冲突"变成"我们**验证了**这个指数测量了冲突"。**这一项性价比最高。**

### 2. 分量消融要在新的风险状态任务上重做

`signal_cleaning_results.json` 的 `feature_ablation` 里有个问题，四个特征组的验证集 F1 几乎相同：

| 特征组 | 验证集 F1 |
|---|---|
| `state_only`（仅状态历史） | 0.818 ← **被选中** |
| `state_activity` | 0.814 |
| `state_semantic` | 0.810 |
| `full` | 0.811 |

被选中的是 `state_only`，也就是**只用状态历史，不用任何语义分量**。

诚实报告出来是好的（`main_feasibility.tex` 的 I-C3 已经这么写了），但它同时挖了个坑：如果三个语义分量对最终预测毫无贡献，那标题里的 "conflict index" 和 I-C1 的整个构造过程还剩多少价值？审稿人会直接指出这个内在矛盾。

**要做的事**：在测试集上补完整的分量消融——不只是特征组，而是逐个移除 A、E、S 三个分量后**重新计算指数、重新走完整流程**，看风险状态分类的 F1/AUC 变化。

两种可能结局都要准备好：

- 若某分量确实有贡献 → 作为 I-C1 的证据。
- 若确实都没贡献 → 必须重新定位论文：语义分量的价值在于**定义了风险状态本身**（阈值基于冲突指数的分位数），而不在于**预测**它。这个论证要显式写出来。

⚠️ 当前 `main.tex:457` 说"完整 A+E+S 融合产生最高 R²"，与新的消融结果不一致，必须改。

### 3. 跨平台泛化（时间允许则做）

现在的正结果只在知乎 20 个主题上。手上已有微博 27 事件反转数据集，把因果清洗 + 风险状态分类那套流程原样搬到微博跑一遍。哪怕结果比知乎差，也能显示方法不是过拟合到单一平台。对 IEEE 期刊分量不轻，但优先级低于前两项。

### 4. 与 competitor 方法的直接对比

`reproduce_competitors/` 目录里只有一个 `demo_random_vs_temporal.py`，没有真正复现的基线。

Introduction 里主张"文献中乐观的预测结果源于数据泄漏"，这是个有力的主张，但需要证据：至少复现一个已发表方法（IPSO-LSTM 或类似），展示它在**随机划分下的高分**与**时序划分下的崩塌**。有了这个，方法论批评就从断言变成了实证。

---

## 三、需要顺手修掉的一致性问题

| 位置 | 问题 |
|---|---|
| `main.tex:289`、表 I 标题 | 说"15 个最活跃主题" |
| `main.tex:414`、表 III 脚注 | 说"20 个主题" |
| `signal_cleaning_results.json` | 实际为 20 个（`n_topics: 20`） |

- **主题数量前后矛盾**：15 vs 20，需统一为 20。
- **表 I 只列了 5 行数据**，却声称覆盖 15 个主题。
- **RQ5 从未定义**：`main.tex:463` 使用了 RQ5（提前预警时间），但 Research Questions 小节只定义了 RQ1–RQ4。
- **孤立图片**：`figures/fig_early_warning.pdf` 已生成，但在 `main.tex` 中从未被引用。

---

## 四、关于投稿目标

仓库里有三个投稿目录：`plos_one_submission`、`srep_submission`、`qingbaoxuebao_submission`。

- **PLOS ONE / Scientific Reports**：以 feasibility 版本的强度（真实数据正结果、置信区间、诚实的边界分析）投这两家是合理的；它们对"方法可靠性 + 明确边界"的论文接受度比追求 SOTA 的会议更高。
- **IEEE 期刊**：会更看重与现有方法的定量对比，也就是待补实验第 4 项。
- **情报学报（中文）**：另一条路，但 `main_cn.tex` 目前只翻译了第三节，缺 Introduction、Related Work、Experiments 和整个参考文献，工作量不小。

---

## 五、建议的执行顺序

1. **构念效度验证**（补全 Wikipedia Detox 数据 + 相关性分析）— 可直接自动完成；中文人工标注需人工参与，可先准备抽样脚本与标注表格。
2. **分量消融重做**（在风险状态任务上，测试集）。
3. **一致性修正**（主题数、RQ5、孤立图片）。
4. **切换主线**到 `main_feasibility.tex`。
5. 跨平台泛化 / competitor 复现（视投稿目标与时间）。

